# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=devices_api

# MongoDB Admin Credentials
# WARNING: Change these passwords in production!
# Use strong, randomly generated passwords for production environments
MONGODB_ROOT_USERNAME=admin
MONGODB_ROOT_PASSWORD=CHANGE_ME_IN_PRODUCTION

# Environment Configuration Notes:
# Local development: Uses MONGODB_URI directly (no auth needed)
# Docker: Uses MONGODB_ROOT_USERNAME/PASSWORD to construct authenticated URI
# Production: ALWAYS use strong passwords and consider using secrets management

# Server Configuration
NODE_ENV=development
PORT=8000